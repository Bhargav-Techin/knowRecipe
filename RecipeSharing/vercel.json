{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "builds": [
    { "src": "angular.json", "use": "@vercel/static-build", "config": { "distDir": "dist/recipe-sharing" } }
  ]
}
